/**
 * @dct/mit-hero-core
 * MIT Hero Core Module - Jest Configuration
 * 
 * Jest configuration for comprehensive testing of the MIT Hero core package
 * 
 * MIT-HERO-MOD: tests added; legacy removed
 */

module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src', '<rootDir>/__tests__'],
  testMatch: [
    '**/__tests__/**/*.test.ts',
    '**/?(*.)+(spec|test).ts'
  ],
  transform: {
    '^.+\\.ts$': 'ts-jest',
  },
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/**/index.ts'
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  },
  setupFilesAfterEnv: [],
  testTimeout: 10000,
  verbose: true,
  clearMocks: true,
  restoreMocks: true
};
